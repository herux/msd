<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/db1_root"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="6dip" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="horizontal" >

            <Button
                android:id="@+id/btn_salescallplan"
                style="@style/HomeButton"
                android:drawableTop="@drawable/salescallplan"
                android:text="Sales Call Plan" />

            <Button
                android:id="@+id/btn_productlist"
                style="@style/HomeButton"
                android:drawableTop="@drawable/productlist"
                android:text="Product List" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="horizontal" >

            <Button
                android:id="@+id/btn_calendar"
                style="@style/HomeButton"
                android:drawableTop="@drawable/calendar"
                android:text="Calendar" />

            <Button
                android:id="@+id/btn_synchronize"
                style="@style/HomeButton"
                android:drawableTop="@drawable/synchronize"
                android:text="Synchronize" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="horizontal" >

            <Button
                android:id="@+id/btn_addcustomer"
                style="@style/HomeButton"
                android:drawableTop="@drawable/customers"
                android:text="Add Customer" />

            <Button
                android:id="@+id/btn_infopromo"
                style="@style/HomeButton"
                android:drawableTop="@drawable/promo"
                android:text="Info Promo" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>